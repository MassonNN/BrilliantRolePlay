{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "command": "${workspaceRoot}/pawno/pawncc.exe",
	//"command": "${workspaceRoot}/pawno/pawn.bat",
    //"isShellCommand": true,
	"args": [
		"${file}",
		"-D${fileDirname}",
		"-O1",
		"-;+",
		"-(+"
	],
	"group": {
		"kind": "build",
		"isDefault": true
	}
}